<?php    $element = $_GET['album'];?><div class="dashboard-main">    <div class="dashboard-title">        <h4 class="white">Images de l'album</h4>    </div>    <div class="dashboard-content">                <div class="d-form-cta header">            <a class="btn-primary btn-add small icon i-more" href="?p=69&album=<?php echo $element ?>">Add an image</a>        </div>                <ul class="list-img-album">            <?php                $stmt = $db->prepare('SELECT id_image, image_url from ALBUMS, ALBUMS_images where id_album = image_album and id_album = ? order by id_image ASC');                $stmt->execute([$element]);                                $results = $stmt->fetchAll(PDO::FETCH_ASSOC);                                foreach ($results as $result) {                    $id_image = $result['id_image'];                    $img = urldecode($result['image_url']);                    echo "                    <li class='img-vignette' data-image-id='$id_image'>                        <img src='$img' width='' height='' alt='' />                         <form action='action_album_img_delete' method='post'>                            <input type='hidden' name='image' value='$id_image'>                            <input type='hidden' name='album' value='$element'>                            <div class='btn-primary btn-delete small icon icon-only'></div>                        </form>                    </li>                    ";                }            ?>        </ul>        </div></div>