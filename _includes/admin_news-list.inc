<div class="dashboard-main">    <div class="dashboard-title">        <h4>Gestion des nouvelles</h4>    </div>    <div class="dashboard-content">                        <a class="btn-primary cta-add small icon mobile-icon-only i-more" href="?p=10"><span class="hover-content">Ajouter une actualit&eacute;</span></a>                <ul class="dashboard-list">            <?php                $stmt = $db->prepare("select * from ACTUALITE order by date_actu DESC");                $stmt->execute();                $results = $stmt->fetchAll(PDO::FETCH_ASSOC);                                foreach ($results as $result) {                    $titre = nl2br(stripslashes($result['titre']));                    $actif = $result['actif'];                    $id_actu = $result['id_actu'];                    if ($actif == 1) {                        $texte_bouton = "Désactiver";                        $btnClass = "btn-disable";                    } else {                        $texte_bouton = "Activer";                        $btnClass = "btn-activate";                    }                    echo "<li class='dashboard-list-elements'>                                    <div class='element-title'>$titre</div>                                    <div class='list-cta'>                                            <form action='action_news_disable' method='post'>                                                <input type='hidden' name='element' value='$id_actu'>                                                <div class='btn-primary $btnClass small icon mobile-icon-only'><span>$texte_bouton</span></div>                                            </form>                                            <form action='index.php?p=12&actu=$id_actu' method='post'>                                                <input type='hidden' name='element' value='$id_actu'>                                                <div class='btn-primary btn-update small icon mobile-icon-only'><span>Modifier</span></div>                                            </form>                                    </div>                                </li>";                }            ?>        </ul>        </div></div>