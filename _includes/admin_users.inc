<div class="dashboard-main">    <div class="dashboard-title">        <h4 class="white">Gestion des utilisateurs</h4>    </div>    <div class="dashboard-content">                        <ul class="dashboard-list">            <?php                                $id_user = $_SESSION['user'];                                $sql_user = $db->prepare('SELECT * FROM USERS WHERE type < 5 AND id_user != ? ORDER BY user_ln ASC');                $sql_user->execute([$id_user]);                                $cpt_user = 0;                while ($row_user = $sql_user->fetch()) {                    extract($row_user);                                        $user_fn = nl2br(stripslashes($user_fn));                    $user_ln = nl2br(stripslashes($user_ln));                    $name = "$user_fn $user_ln";                    $username = $email;                                        // Create an array of user types and their corresponding activate classes                    $userTypes = [                        1 => 'User',                        2 => 'Coach',                        3 => 'Staff',                        4 => 'Registraire'                    ];                                        $cpt_user++;                    echo "            <li class='dashboard-list-elements list-user'>                <div class='list-cta list-user-delete'>                    <div class='cta-user delete'>                        <form action='action_users_update' method='post'>                            <input type='hidden' name='num_user' value='$id_user'>                            <input type='hidden' name='type_user' value='0'>                            <div class='btn-tertiary btn-text small btn-delete icon mobile-icon-only'></div>                        </form>                    </div>                </div>                <div class='user-name'><span>$name</span> - email : $username</div>                <div class='list-cta list-user-type'>                ";                                        // Loop through the user types to generate the buttons                    foreach ($userTypes as $typeValue => $userTypeLabel) {                        $activateClass = ($type == $typeValue) ? 'activate' : '';                                                echo "<div class='cta-user $activateClass'>                    <form action='action_users_update' method='post'>                        <input type='hidden' name='num_user' value='$id_user'>                        <input type='hidden' name='type_user' value='$typeValue'>                        <input type='submit' value='$userTypeLabel' />                    </form>                </div>";                    }                                        echo "                </div>            </li>";                }            ?>        </ul>        </div></div>