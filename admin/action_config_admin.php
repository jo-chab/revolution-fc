<?php    include("../_includes/session_test.inc");    include("../_includes/_revolutionfc.inc");    require_once '../vendor/ezyang/htmlpurifier/library/HTMLPurifier.auto.php'; // Include HTML Purifier        unset($_GET);        $websiteUrl = isset($_POST['zt_url']) ? nl2br(stripslashes($_POST['zt_url'])) : retour_erreur(2, 23);    $root = isset($_POST['zt_path']) ? nl2br(stripslashes($_POST['zt_path'])) : retour_erreur(2, 23);        $userHtml = isset($_POST['ta_service']) ? $_POST['ta_service'] : retour_erreur(2, 23);        // Create an HTML Purifier configuration    $config = HTMLPurifier_Config::createDefault();    $purifier = new HTMLPurifier($config);        // Clean and sanitize the HTML content    $cleanHtml = $purifier->purify($userHtml);        $stmt = $db->prepare('UPDATE CONFIG_admin SET config_url = ?, config_path = ?, text = ? WHERE id_config = 1');    $execute = $stmt->execute([        $websiteUrl,        $root,        $cleanHtml    ]);        if ($execute) {        $stmt->closeCursor();        retour_erreur(2, 3);    } else {        retour_erreur(2, 4);    }    ?>