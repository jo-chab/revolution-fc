<?php	include("../_includes/session_test.inc");	include("../_includes/_revolutionfc.inc");    include("../_includes/_fileUpload.inc");    unset($_GET);$requiredFields = ['zt_title', 'zt_intro', 'ta_prog'];        foreach ($requiredFields as $field) {        if (isset($_POST[$field])) {            ${$field} = nl2br(stripslashes($_POST[$field]));                        if ($field === 'ta_prog') {                $ta_prog_clean = str_replace("<br /><br />", "<br />", $ta_prog);            }        } else {            retour_erreur(51, 7);        }    }            // Define the valid file extensions    $extensions_valides = array('jpg', 'jpeg', 'png');    $page = '51';    $error_code = '1024';        // Handle file upload    $dossier = "img/";    $destination = handleFileUpload($_FILES['f_photo_p'], $dossier, $extensions_valides, $page, $error_code);    $stmt = $db->prepare('INSERT into PROGRAMME (programme_name, programme_intro, programme_text, programme_img) VALUES (?, ?, ?, ?)');    $execute = $stmt->execute([        $zt_title,        $zt_intro,        $ta_prog_clean,        $destination    ]);    if ($execute) {        $stmt->closeCursor();        retour_erreur(51,501);    }    else {        retour_erreur(51,502);    }?>